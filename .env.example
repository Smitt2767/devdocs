# -------------------------------------------------------
# App
# -------------------------------------------------------

# The public base URL of the site. Used in sitemaps, OG images, and canonical URLs.
# Local:  http://localhost:3000
# Dev:    https://dev.yoursite.com
# Prod:   https://yoursite.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# The current deployment environment.
# Controls sitemap generation and search engine indexing.
# Allowed values: local | dev | prod
# Only "prod" enables the sitemap and allows crawlers.
NEXT_PUBLIC_ENVIRONMENT=local